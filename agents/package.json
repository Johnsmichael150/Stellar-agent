{
  "scripts": {
    "typecheck": "tsc --noEmit -p registry/tsconfig.json && tsc --noEmit -p buyer/tsconfig.json && tsc --noEmit -p seller-webbuilder/tsconfig.json && tsc --noEmit -p seller-copywriter/tsconfig.json"
  },
  "dependencies": {
    "express-rate-limit": "^8.5.2"
  }
}
